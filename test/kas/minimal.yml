header:
  version: 14

machine: qemux86-64
distro: poky
target: core-image-minimal

# Minimal test configuration for CI
# Uses a stable Yocto LTS release (kirkstone) for reliable testing
repos:
  poky-test:
    url: https://git.yoctoproject.org/poky
    branch: kirkstone
    layers:
      meta:
      meta-poky:
